# Ambiente virtual
venv/

# Variáveis de ambiente (senhas, chaves de API — nunca subir para o GitHub!)
.env

# Cache do Python
__pycache__/
*.pyc

# Arquivos do sistema (Mac)
.DS_Store

# PDFs das bulas (não versionados — conteúdo já extraído em bulas_index.json)
# Para regenerar o índice, baixe os PDFs manualmente e rode:
#   python3 -m backend.rag.pdf_extractor
backend/rag/pdfs/
